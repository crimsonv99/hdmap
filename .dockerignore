node_modules
.npm
Dockerfile
.dockerignore
.git
.gitignore
.DS_Store
*.py

# Secret — supplied at runtime via the API_KEYS env var, never baked into the image.
keys.json

# Regenerable / heavy build products the runtime doesn't need.
# KEEP tiles/ and data/graph.json — the app serves those.
build/
data/*.pmtiles
data/*.geojson
index-district.html
task_list.md
